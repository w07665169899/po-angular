import { PoChartTooltipDirective } from './po-chart-tooltip.directive';

describe('PoChartTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new PoChartTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
